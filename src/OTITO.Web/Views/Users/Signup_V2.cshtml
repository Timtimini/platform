@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@using Microsoft.Extensions.Options
@using reCAPTCHA.AspNetCore
@using reCAPTCHA.AspNetCore.Templates
@model OTITO.Web.Models.Authentication.UserSignup
@inject IOptions<RecaptchaSettings> RecaptchaSettings

<form method="post" action="@Url.Action("Signup_V2", "Users")">
    <div style="height:200px"></div>
    @if (TempData["ErrorMessage"] != null)
    {
        <div style="color:red; border:red;padding:20px">
            @TempData["ErrorMessage"]
        </div>
    }
    <div class="row">
        <div class="col-lg-6">
            <div class="modal-input">
                <img src="~/images/mail.svg" width="24" height="18" style="margin-top:16px; margin-left:10px;" />
                @Html.EditorFor(x => x.Email, new { style = "align-self:stretch;" })
            </div>
            <div class="row delegate" style="color:red;font-size:14px;">

                @Html.ValidationMessageFor(x => x.Email)
            </div>
            <div class="modal-input">
                <img src="~/images/lock.svg" width="18" height="26" style="margin-top:13px; margin-left:10px;" />
                @Html.EditorFor(x => x.Password, new { style = "align-self:stretch;" })
            </div>
            <div class="row delegate" style="color:red;font-size:14px;">
                @Html.ValidationMessageFor(x => x.Password)
            </div>

            <div class="modal-input">
                <img src="~/images/lock.svg" width="18" height="26" style="margin-top:13px; margin-left:10px;" />
                @Html.EditorFor(x => x.ConfirmPassword, new { style = "align-self:stretch;" })

            </div>
            <div class="row delegate" style="color:red;font-size:14px;">
                @Html.ValidationMessageFor(x => x.ConfirmPassword)
            </div>

            @(Html.Recaptcha<RecaptchaV2Checkbox>(RecaptchaSettings.Value))

            <div class="row delegate">
                <div class="col-lg-10">
                    <button class="btn btn-success" type="submit">
                        Signup
                    </button>
                </div>
            </div>

        </div>
        <div class="col-lg-6">
            <button class="btn btn-facebook col-lg-10">
                <img src="~/images/facebok_icon_white.svg" width="20" height="20" />
                Join with Facebook
            </button>
            <button class="btn btn-twitter col-lg-10">
                <img src="~/images/twitter_icon_white.svg" width="20" height="20" />
                Join with Twitter
            </button>
            <button class="btn btn-google col-lg-10">
                <img src="~/images/google_icon_white.svg" width="20" height="20" />
                Join with Google
            </button>
            <button type="button" class="btn btn-success col-lg-10" style="margin-top:10px" onclick="window.location.href='/Users/Login'">
                Login
            </button>
        </div>
    </div>
</form>